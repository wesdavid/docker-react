language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t weslleydavid/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true weslleydavid/docker-react npm run test -- --coverage
